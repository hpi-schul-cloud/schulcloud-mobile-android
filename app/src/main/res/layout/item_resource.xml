<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewHolder"
            type="org.schulcloud.mobile.controllers.topic.ResourceListAdapter.ResourceViewHolder" />
        <variable
            name="resource"
            type="org.schulcloud.mobile.models.content.Resource" />
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardUseCompatPadding="true"
        android:foreground="?android:attr/selectableItemBackground"
        android:onClick="@{() -> viewHolder.openExternal()}">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="16dp"
                android:layout_marginStart="16dp"
                android:layout_marginTop="8dp">

                <TextView
                    style="@style/Content_Header.WithLink"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:background="@null"
                    android:text="@{resource.title}"
                    android:textColor="@color/hpiRed"
                    tools:text="Resource title" />

                <ImageView
                    style="@style/Content_LinkExternal"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    app:srcCompat="@drawable/ic_open_in_new_dark_24dp" />

            </LinearLayout>

            <org.schulcloud.mobile.views.PassiveWebView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="16dp"
                android:layout_marginStart="16dp"
                android:layout_marginTop="8dp"
                app:content="@{resource.description}"
                tools:ignore="WebViewLayout" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:background="@drawable/sc_card_footer"
                android:ellipsize="end"
                android:lines="1"
                android:paddingBottom="8dp"
                android:paddingEnd="16dp"
                android:paddingStart="16dp"
                android:paddingTop="8dp"
                android:text="@{@string/content_resource_client(resource.client)}"
                tools:text="via Client" />

        </LinearLayout>

    </android.support.v7.widget.CardView>
</layout>
