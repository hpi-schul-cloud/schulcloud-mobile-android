/*
package org.schulcloud.mobile.models.file

import org.junit.Before

import org.junit.Assert.*
import org.junit.Test

class DirectoryResponseTest {
    private companion object {
        val FILES = emptyList<File>()
        val DIRECTORIES = emptyList<Directory>()
    }

    private val newDirectoryResponse: DirectoryResponse
        get() = DirectoryResponse().apply {
            files = FILES
            directories = DIRECTORIES
        }
    private lateinit var directoryResponse: DirectoryResponse

    @Before
    fun setUp() {
        directoryResponse = newDirectoryResponse
    }

    @Test
    fun testGetProperties() {
        assertEquals(FILES, directoryResponse.files)
        assertEquals(DIRECTORIES, directoryResponse.directories)
    }
}
*/
