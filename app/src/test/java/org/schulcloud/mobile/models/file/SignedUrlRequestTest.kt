/*
package org.schulcloud.mobile.models.file

import org.junit.Assert.*
import org.junit.Before
import org.junit.Test

class SignedUrlRequestTest {
    private companion object {
        const val ACTION = "action"
        const val PATH = "path"
        const val FILETYPE = "fileType"
    }

    private val newSignedUrlRequest: SignedUrlRequest
        get() = SignedUrlRequest().apply {
            action = ACTION
            path = PATH
            fileType = FILETYPE
        }
    private lateinit var signedUrlRequest: SignedUrlRequest

    @Before
    fun setUp() {
        signedUrlRequest = newSignedUrlRequest
    }

    @Test
    fun testGetProperties() {
        assertEquals(ACTION, signedUrlRequest.action)
        assertEquals(PATH, signedUrlRequest.path)
        assertEquals(FILETYPE, signedUrlRequest.fileType)
    }
}
*/
